# Handoff — 2026-07-01 13h30

## Estado em voo
Nada em andamento. Sessão foi curta e ponta-a-ponta: leitura de email da Groq → issue → fix → commit → push → close. Sem trabalho pendente.

## Issues abertas relevantes
Nenhuma aberta por esta sessão. Issue #24 ("Migrar Obi-Wan de llama-3.3-70b-versatile antes de 2026-08-16") foi aberta, corrigida e fechada na mesma sessão (commit `aa4459a`).

## Guard-rails ativos
- **Groq deprecou `llama-3.3-70b-versatile`**: decommission em **2026-08-16**. Obi-Wan já migrado para `openai/gpt-oss-120b` (config.py, api.py, model_router.py) — mas o **deploy em produção na Polaris** (systemd) ainda não foi feito. O código no repo está atualizado; o processo rodando na Polaris continua com o binário/env antigo até reiniciar o serviço. Se `GROQ_MODEL` estiver setado explicitamente no `.env` do systemd da Polaris (`/opt/obi-wan/.env`), o default novo no código não terá efeito — checar isso antes de decommission.
- **Qwen3-32B também já foi deprecado pela Groq** (17/06/2026) a favor do gpt-oss-120b — não usar Qwen como alternativa futura sem checar de novo.
- `.vault`: usar sempre `grep '^GITHUB_TOKEN='` (match exato), não `grep GITHUB_TOKEN` — esse grep solto casa também com `GITHUB_TOKEN_LAB` e concatena os dois valores, quebrando a auth da API do GitHub.

## Próxima ação recomendada
Antes de 2026-08-16: confirmar/atualizar `GROQ_MODEL` no `.env` de produção da Polaris (systemd `/opt/obi-wan/.env`) e reiniciar o serviço `obi-wan/api.service` para pegar `openai/gpt-oss-120b`. Sem isso, o Obi-Wan para de responder no dia do decommission mesmo com o código do repo já corrigido.

## Decisões desta sessão
- Substituto escolhido: `openai/gpt-oss-120b` (não Qwen — já deprecado na Groq). Verificado via GroqDocs ao vivo, não por memória.
- `*.code-workspace` adicionado ao `.gitignore` (artefato local de IDE, não pertence ao repo).
