# Handoff — 2026-06-30 22h

## Estado em voo

Sessão focada em Claude Code usage monitoring. Sistema completo e funcional — aguarda validação do primeiro checkpoint real (22h).

## Issues abertas relevantes

- **#22** — Validar checkpoint 22h (rate_limits.seven_day) — **PRIORIDADE**: checar se a mensagem chegou correta
- **#23** — Webhooks nos 7 planetas sem cobertura — bloqueado por WEBHOOK_SECRET ausente no .vault
- **#17** — SbrTask divergência Lab-Sobral-Dev vs paulinett1508-dev
- **#18** — Convenção de workspace local para repos de planetas

## Guard-rails ativos

- **NÃO usar ccusage para % semanal** — subreporta 30-50%. Fonte de verdade: `rate_limits.seven_day` do statusline JSON.
- **rate-limits.json só existe se Claude Code estiver ativo** — se o script rodar sem sessão aberta, ele aborta com aviso. Normal.
- **WEBHOOK_SECRET do .vault** — buscar em `/opt/obi-wan/.env` na Polaris antes de rodar setup-webhooks.py. Nunca inventar o secret.
- **Lab-Sobral-Dev agora tem só 4 repos** (eram 8) — serverIA, serverpfsense, gestao-sbr, SBR-ocomon-5.0 foram excluídos. amilcar.md já atualizado.

## Próxima ação recomendada

1. Checar Telegram às 22h — mensagem deve mostrar ~57-60% semanal
2. Se correto: fechar #22
3. Buscar WEBHOOK_SECRET em `/opt/obi-wan/.env` e adicionar ao `.vault`
4. Rodar `python scripts/setup-webhooks.py` → fechar #23
5. Seguir para frente #7 (cinturões no dashboard) ou #8 (SbrTask sync)

## Decisões desta sessão

**Rate limits via statusline hook (não ccusage):**
O `statusline.sh` agora grava `~/.claude/state/rate-limits.json` a cada interação — contém `seven_day.used_percentage` direto da Anthropic. O script de Telegram lê esse arquivo. Elimina completamente o ccusage para métricas semanais.

**Telegram 3x/dia, não digest contínuo:**
Checkpoints em 12h, 18h, 22h BRT. Task Scheduler com 3 tasks separadas (Claude-Usage-12h00/18h00/22h00). Python 3.13 global com path absoluto.

**Canal Obi-Wan como oficial para usage:**
Sem bot dedicado (rate limit BotFather, e não faz sentido bot só pra isso). Obi-Wan manda direto no chat privado do TheGod via SOL_CHAT_ID.

**Lab-Sobral-Dev — 4 repos reais:**
BI-sobral, Projeto-scale, SBR-KPIs, SbrTask. Restante foi excluído. amilcar.md corrigido.
