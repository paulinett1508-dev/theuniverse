# Handoff — 2026-07-01 17h

## Estado em voo
Nada em andamento. Issue #18 (convenção de workspace local) resolvida ponta-a-ponta nesta sessão: doc escrita, script criado, 18 planetas Kuiper clonados fisicamente, commit `1c5db09` pushed, issue fechada.

## Issues abertas relevantes
- **#25** (nova) — definir local canônico de clone para Gould/Van Allen (cinturões profissionais). TheGod confirmou que já existe local pra eles, mas não documentado. Sem essa decisão, `docs/ecossistema/workspace-convention.md` só cobre Kuiper.
- #17 — divergência SbrTask (Lab-Sobral-Dev vs paulinett1508-dev), ainda aberta, não tocada nesta sessão.

## Guard-rails ativos
- **`corpos/` é só Kuiper.** Não rodar `python scripts/workspace.py` esperando que ele clone Gould/Van Allen — o script filtra por `NATUREZA.get(name) in {"kuiper"}` de propósito (ver `scripts/workspace.py`). Se a #25 for resolvida mudando esse escopo, atualizar `ESCOPO` no script.
- **Não reclonar planetas profissionais dentro do theuniverse.** Já rolou um engano nesta sessão (script clonou os 37 antes de eu perceber o escopo errado) — teve que ser abortado e limpo (`rm -rf corpos/gould corpos/van-allen`). Amilcar/institucional já tem local de trabalho estabelecido fora daqui.
- `temperodemamae` tem cinturão `nao-classificado` no Censo — não entra em `corpos/` até ser classificado (não abri issue pra isso, é bem menor que a #25; mencionar se aparecer de novo).
- Token do GitHub nunca fica gravado em `.git/config` dos clones — `scripts/workspace.py` usa `x-access-token@` só na hora do `git clone` e reescreve o remote pra URL limpa depois.

## Próxima ação recomendada
Se o TheGod quiser fechar a #25: perguntar direto onde ficam os clones de Amilcar hoje (ex: pasta fixa no disco, ou workspace do lado do Lab-Sobral-Dev) e documentar em `docs/ecossistema/workspace-convention.md`. Não presumir path — é exatamente o erro que gerou a #18 original.

## Decisões desta sessão
- **`D:\theuniverse` é a raiz física oficial.** Clones vivem em `corpos/<cinturão>/<cluster>/<planeta>`, git-ignorado, espelhando 1:1 a classificação do Censo (`NATUREZA` + `CLUSTERS` em `scripts/censo.py`) — nada de taxonomia paralela.
- **Escopo inicial: só Kuiper (18 planetas).** Gould e Van Allen ficam de fora por decisão do TheGod — já têm home canônico, evitar duplicar clone do mesmo repo em dois lugares.
- Para codar qualquer planeta Kuiper: abrir `corpos/kuiper/<cluster>/<planeta>` diretamente — é o repo real, `.git` próprio, sem relação com o git do theuniverse.
