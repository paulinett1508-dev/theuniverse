# Handoff — 2026-07-01 17h30

## Estado em voo
Nada em andamento. Sessão curta: validação pós-#18. Explicado o fluxo de setup em computador novo (clone do theuniverse não traz `corpos/kuiper/`, precisa `.vault` + `scripts/workspace.py`) e documentado no `ESTADO.md` (commit `4f6dff0`). Rodado `scripts/workspace.py` aqui mesmo pra confirmar idempotência: 18/18 já clonados, 0 a clonar, sem erro.

## Issues abertas relevantes
- **#25** — definir local canônico de clone pra Gould/Van Allen. Segue aberta, decisão do TheGod.
- #17 — divergência SbrTask. Não tocada.

## Guard-rails ativos
- Mesmos da sessão anterior (ver `docs/handoffs/2026-07-01-17h.md`): `corpos/` é só Kuiper, não expandir `ESCOPO` no `scripts/workspace.py` sem decisão do TheGod; não reclonar Gould/Van Allen dentro do theuniverse.
- **Confirmado nesta sessão:** `scripts/workspace.py` é idempotente na prática, não só na intenção — rodar de novo sem `--dry-run` não reclona nem quebra nada quando tudo já existe.

## Próxima ação recomendada
Nenhuma pendente. Se o TheGod quiser fechar a #25, é decisão dele sobre onde ficam os clones profissionais.

## Decisões desta sessão
- `ESTADO.md` → seção "Setup em novo computador" ganhou o passo 5: rodar `scripts/workspace.py` após recriar o `.vault` pra rematerializar `corpos/kuiper/` numa máquina nova.
