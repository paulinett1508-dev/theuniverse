# Handoff — 2026-06-25 21h

## Estado em voo

Sessão encerrou após estruturar a constelação Amilcar no theuniverse. O prompt para continuar o trabalho dentro do repo `the-matrix` foi gerado e entregue ao TheGod. Nenhum arquivo do `the-matrix` foi tocado — os ajustes internos (renomear repo, reestruturar manifesto) são papel daquele repo.

## Issues abertas relevantes

- **#10** `sbrgestao: Estrela da Morte — agnvendas-unit-tests failure` — CI vermelho, alerta ativo no dashboard
- **#13** `arquitetura: James Webb — segundo observatório` — trigger-based, sem prazo

## Guard-rails ativos

- **Não renomear `the-matrix` pelo theuniverse** — ação destrutiva (URLs quebram, submodules afetados). Deve ser executada de dentro do repo `the-matrix` com sessão própria.
- **`traefik-myri-traefik-1` em crash loop na Polaris** — não tocar em infra de produção sem investigar. Container reiniciando há pelo menos 16s no momento do fechamento. Verificar antes de qualquer deploy.
- **`.vscode/settings.json` com customizações de cor removidas** — VS Code limpou automaticamente. Restaurar manualmente se necessário (ver `git diff HEAD~4 .vscode/settings.json`).
- **Obi-Wan, antigravity e webhook rodam via systemd** — não são Docker. Ausência do `docker ps` é normal.

## Próxima ação recomendada

Abrir sessão no repo `the-matrix` com o prompt gerado nesta sessão e executar a reestruturação interna da constelação Amilcar. Depois investigar o crash do `traefik-myri-traefik-1` via `ssh vps "docker logs traefik-myri-traefik-1 --tail 50"`.

## Decisões desta sessão

- **Constelações** — nova categoria cosmológica introduzida no universo. Agrupamentos de repos com identidade gravitacional própria, acima dos planetas na hierarquia.
- **Amilcar** — nome escolhido pelo TheGod para a constelação do Lab Sobral. Alma de Trantor (Foundation/Asimov). Nome carrega peso humano; alma carrega peso galáctico.
- **Telegram via bot token** — não usar o token do Obi-Wan para enviar perguntas ao Obi-Wan. O bot não processa suas próprias mensagens. Para interações com Obi-Wan: usuário envia diretamente, ou Guardião prepara mensagem formatada para o usuário colar.
- **Broadcast de skills** — padrão estabelecido: `scripts/broadcast_skills.py` cria issues em repos-alvo via `gh issue create --repo`. Reutilizar para próximos broadcasts.
- **`/abrirsessao` e `/fecharsessao`** — commands registrados em `.claude/commands/`, issues abertas nos 19 repos do ecossistema para instalação.
