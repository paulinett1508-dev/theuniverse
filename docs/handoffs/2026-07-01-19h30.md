# Handoff — 2026-07-01 19h30

## Estado em voo
Nada em andamento. Sessão de centralização de workspace: rodado `scripts/workspace.py` nesta máquina (nunca tinha sido rodado aqui) — 18/18 planetas Kuiper clonados de uma vez em `corpos/kuiper/<cluster>/<planeta>` (nenhum existia antes localmente).

Durante o processo, quase indiquei local errado pro `sigmed` (inferi Van Allen a partir de aparecer no roster antigo da Amilcar) — corrigido por outro dev via chat, duas vezes, antes de eu confirmar contra `scripts/censo.py` (`NATUREZA["sigmed"] = "kuiper"`). Puxados os commits `9ed36a8` (glossário dos 3 eixos: planeta genérico / cinturão / papel-na-Amilcar) e `8345b7e` (remoção formal do `sigmed` do roster da Amilcar) que já estavam no remoto documentando essa correção.

Também: `.vercel/` (link local do Vercel CLI, existia desde 23/06 sem estar no `.gitignore`) foi adicionado ao `.gitignore` e commitado (`a53aae2`, rebaseado sobre `18dd669` e pushado).

## Issues abertas relevantes
Nenhuma tocada nesta sessão. Backlog inalterado: #23, #22, #21, #20, #19, #17, #16, #13 — todas "proxima-sessao" ou sem urgência nova.

## Guard-rails ativos
- **Cinturão (kuiper/gould/van-allen) nunca se infere pelo papel-na-Amilcar** (roster de `constellations/*.md`). Fonte de verdade: `NATUREZA` em `scripts/censo.py` + campo `cinturão` na ficha `planets/<nome>.md`. Ver glossário no `CLAUDE.md`.
- `sigmed` = Kuiper, cluster `sobral-core`, fora da constelação Amilcar. Local canônico: `corpos/kuiper/sobral-core/sigmed`.
- **A partir de agora, todo planeta Kuiper se abre por `corpos/kuiper/<cluster>/<planeta>`** dentro do theuniverse — não usar clones soltos antigos.
- `D:\PROJETOS\MY-JOBS\sigmed` foi deliberadamente abandonado (não apagado) como possível consulta futura — não reabrir pra codar.
- `corpos/` e `.vercel/` são gitignorados por design — não tentar commitar.

## Próxima ação recomendada
Nenhuma pendente desta sessão. Backlog de sessões anteriores continua disponível pra retomar (#16 traefik crash loop é o único com bug confirmado ao vivo — Restarting no vps agora mesmo).

## Decisões desta sessão
- Workspace físico centralizado: `corpos/kuiper/` é a raiz única pra planetas Kuiper nesta máquina, daqui pra frente.
- Clone legado `MY-JOBS/sigmed` fica só como resíduo de consulta, sem uso ativo.
- `.vercel/` passa a ser gitignorado (chore, sem impacto funcional).
