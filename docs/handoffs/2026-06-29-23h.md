# Handoff — 2026-06-29 23h

## Estado em voo

Sessão de retomada após compactação de contexto. Tudo que foi planejado na sessão anterior foi implementado: formato v2 de notificações aplicado em todos os 7 scripts (`pulso`, `deps`, `deploy_health`, `secret_scan`, `model_scout`, `artoo_voice`, `weekly-usage-notify`), e o Fleet HUD adicionado ao dashboard com canvas animado mostrando Obi-Wan + 9 satélites em órbita.

O Fleet HUD está **deployed e funcionando** em https://theuniverse-lake.vercel.app — mas há colisão de layout com elementos existentes que precisa ser resolvida na próxima sessão dedicada ao dashboard.

## Issues abertas relevantes

- **#19** — `[DASHBOARD]` Reorganizar layout fixo — fleet HUD + sidebar sobrepondo elementos → **prioridade alta para próxima sessão de dashboard**
- **#20** — `[BOTS]` Criar 5 bots restantes no BotFather e migrar workflows
- **#21** — `[BOTS]` Criar tópicos Telegram faltantes (2, 4, 6, 14, 16) no grupo
- **#18** — docs: definir convenção de workspace local
- **#17** — SbrTask: investigar divergência Lab-Sobral-Dev vs paulinett1508-dev

## Guard-rails ativos

- **BotFather rate limit:** máximo ~2 bots por sessão antes do bloqueio de 23h. Criar em batches de 2, pausar entre cada.
- **Tópicos Telegram:** somente IDs 8 (pulso), 10 (deps), 12 (deploy) confirmados existentes. Não testar envio nos IDs 2, 4, 6, 14, 16 antes de criar os tópicos manualmente no grupo.
- **Dashboard — não modificar posicionamento** sem primeiro mapear todos os `position:fixed` e coordenadas. Ver issue #19.
- **state/artoo-state.json:** sempre `git pull --rebase` antes de push — Actions faz commit automático nesse arquivo a cada run.
- **Tokens `.vault`:** nunca commitar. Nunca.

## Próxima ação recomendada

**Sessão de dashboard:** abrir `dashboard/index.html`, listar todos os elementos `position:fixed` com suas coordenadas, montar mapa de colisões, depois reorganizar. Começar pela colisão mais óbvia: `#msg-toast` e `#obiwan-hud` estão ambos em `bottom:80px right:24px`.

**Sessão de bots:** criar os 2 primeiros dos 5 bots pendentes no BotFather (`universo_nervoso_bot` e `universo_farejador_bot`), salvar tokens em `.vault` + GitHub Secrets, migrar `sentinel.yml` e `secret-audit.yml`.

## Decisões desta sessão

- **Formato v2 de notificações:** adotado o padrão `└ tree + <code> inline` para todos os scripts. Sem `<pre>` blocks (quebra linha no mobile). BRT sempre via `datetime.utcnow() - timedelta(hours=3)`.
- **Arquitetura de bots:** cada sentinela tem token dedicado. Obi-Wan é gateway inbound puro — não recebe notificações de saída para evitar loop.
- **Fleet HUD:** canvas animado com Obi-Wan como hexágono central + Artoo em órbita interna + 8 sentinelas em órbita externa. `setFleetBotStatus(id, state)` exposto globalmente para integração futura com eventos reais.
- **ESTADO.md** não atualizado nesta sessão — atualizar no início da próxima com frente "dashboard layout" como ativa.
